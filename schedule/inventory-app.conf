# inventory-app -- vaporwave self-dev (realisateur#1131). Host notes: _paced.vaporwave.conf.
# WHAT IT IS: vkv-inventory (media-arts-collective/inventory-app), migrating off
# svc-vaporwave (uid 1001, mandark) as mandark is decommissioned (realisateur#695).
# Private repo, Apps Script project (.clasp.json/appsscript.json): push works,
# deploy is domain-refused for an account outside nomac.org (see mandark notes).
# Zero open issues as of 2026-09-08 -- expected, not a fault; DONE and stop.
PROJECT="inventory-app"
PROJECT_KEY="inventory-app"
CRON_HOST="vaporwave"
CRON_ACCOUNT="inventory-app"
PROJECT_REPO_PATH="$HOME/Documents/Projects/inventory-app"
REPO_URL="https://github.com/media-arts-collective/inventory-app.git"
GH_OWN_REPO="media-arts-collective/inventory-app"
REPO_SUBDIR="."
ANSWER_CHANNEL="issues"
SWEEP_JOB_NAME=""
BATCH_JOB_NAME="inventory-app-nightly-batch"
BATCH_MAX_TURNS="120"
USES_STANDING_RULES=1
