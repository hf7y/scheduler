PROJECT="abc"                                        # music for the Abecedarian trilogy: three feature films meant to play simultaneously
PROJECT_KEY="abc"                                    # the repo holds LilyPond notation; media and the film's data model live on dexter

CRON_HOST="monkey"                                   # registered 2026-09-23; standing-up tracked in hf7y/realisateur#1277
CRON_ACCOUNT="abc"                                   # account exists: uid 3020, group selfdev, deploy keys wired
SELFDEV_IN_ACCOUNT=0

PROJECT_REPO_PATH="$HOME/Documents/Projects/abc"
REPO_URL="https://github.com/hf7y/abc.git"           # default branch is `main`, renamed from `master` 2026-09-23 so neither etalon's guard.yml fetch nor scheduler-run's default-branch resolution hits the realisateur#39 trap
REPO_SUBDIR="."

SWEEP_JOB_NAME=""
BATCH_JOB_NAME="abc-nightly-batch"
BATCH_PROMPT="@@FRAGMENT:run-procedure@@"            # shared fragment, per gardien's precedent; BATCH_CRON omitted because gardien omits it
BATCH_MAX_TURNS="120"
USES_STANDING_RULES=1                                # queue is real before it dispatches: 10 open issues across 4 milestones, prose/prose required on main
