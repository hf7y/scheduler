# Participants for usage-paced-runner.sh -- HOST-SCOPED to `monkey`.
#
# Format and semantics: see schedule/_paced.conf's header. Not repeated here.
#
# WHY THIS HOST EXISTS (2026-08-03, Zach-directed; realisateur/MONKEY.md):
# self-dev leaves mandark. `monkey` is a VirtualBox guest on dexter's Windows
# host, one unix user per project, and this is the file that decides what it
# dispatches. `usage-paced-runner.sh` resolves _paced.$(hostname -s).conf and
# FALLS BACK to the shared _paced.conf when there is no host file -- on a new
# host that fallback is not a default, it is mandark's rotation. This file
# existing is what makes that impossible.
#
# THREE ENABLED ROWS since 2026-08-04 (ecosim, bibliothecaire, vim-arcade).
# The one-row rule this header used to state ("before there is a second, the
# dispatcher topology has to be decided") was answered rather than waived --
# realisateur/MONKEY.md section 9.1, Zach-directed. Row #3 is the one that
# section's "before a third dispatcher" checklist named: the per-account
# `$HOME`-scoped lock is unchanged and not serialised host-wide (still true),
# but nothing further was done here because that residual was already priced
# in when #9.1 was written, not discovered by adding this row.
#
# The short version, because the answer is the reason a reader may add a
# third row without asking: N dispatchers was never the thing protecting the
# quota. `bin/usage-gate.sh` is, and it protects it by probing Anthropic's
# unified rate-limit headers, which are ACCOUNT-WIDE -- every host, every unix
# user, every interactive session, already true of mandark+dexter sharing one
# budget. A second account sees the first account's spend. Adding rows scales
# how many things ask permission, not what they are asking about.
#
# THE ONE THING TO CHECK BEFORE ROW #3. `usage-paced-runner.sh` locks
# `$HOME/.local/share/$JOB_NAME/run.lock` -- $HOME-scoped, so per ACCOUNT.
# Two accounts can dispatch in the same tick, and nothing on this host
# serialises them.
#
# CORRECTED 2026-08-04. This paragraph used to say the race was "bounded by
# USAGE_CEILING (0.85, i.e. 15% of headroom to eat)". That was wrong, and it
# was wrong in the direction that matters: 0.85 is usage-gate.sh's BUILT-IN
# default, and schedule/_usage.conf overrides it. What is actually configured:
#
#   USAGE_CEILING=0.99                  ~1% of headroom, not 15%
#   USAGE_RUSH_BEFORE_RESET_MIN=10080   10080 minutes = 7 days = the WHOLE
#                                       7-day window
#
# The second is the one to understand. usage-gate.sh drops the even-burn
# "on-pace" hold entirely within that many minutes of the 7-day reset -- so at
# 10080 the pacing brain is permanently off, and every gate line on this host
# reads `rush=True`. Pacing is not what is protecting the quota here; a 0.99
# ceiling and a `rejected` status are, and that is all.
#
# So before arming a third dispatcher, the question is not "does the ceiling
# absorb the race" -- at 1% headroom, with no pacing hold, it barely absorbs
# the second. Either serialise the accounts (a host-wide lock rather than a
# $HOME-scoped one) or put the pacing hold back by lowering
# USAGE_RUSH_BEFORE_RESET_MIN to something like its documented 120.
#
# DO NOT ACT ON THAT LAST CLAUSE WITHOUT READING schedule/_usage.conf FIRST.
# Corrected 2026-08-05, after this file's advice was nearly followed.
#
# `USAGE_RUSH_BEFORE_RESET_MIN=10080` is not drift and not an oversight. It is
# a Zach-directed decision of 2026-07-29, recorded in _usage.conf with its
# evidence: "remove the burn line for the 7 day window. I have usage credits
# for margin over 5h window." It was made because BOTH hosts had stalled --
# mandark's last dispatch was 17h old and dexter had dispatched ZERO jobs ever
# (463 HOLD, 0 RUN in its entire log) -- while both gates read a comfortable
# "29% used vs burn-line 27%, on-pace". Even-burn pacing was holding the
# automation down while interactive sessions spent the same weekly budget.
#
# Lowering it back re-creates exactly that stall, immediately. Probed rather
# than predicted, 2026-08-05 with six dispatchers armed and 7d util=0.55:
#
#   $ USAGE_RUSH_BEFORE_RESET_MIN=120  ./bin/usage-gate.sh -> verdict=HOLD
#   $ USAGE_RUSH_BEFORE_RESET_MIN=1440 ./bin/usage-gate.sh -> verdict=HOLD
#   $ USAGE_RUSH_BEFORE_RESET_MIN=4320 ./bin/usage-gate.sh -> verdict=HOLD
#   (hold_reasons=7d:on-pace -- the estate is genuinely above even-burn pace)
#
# Every value that restores pacing stops ALL SIX dispatchers on the spot, and
# keeps them stopped until burn falls under the line or the window resets. That
# may one day be the right call; it is a QUOTA POLICY decision that belongs to
# Zach and to _usage.conf, and it is not a knob to turn while tidying a
# rotation file. What genuinely remains unaddressed here is the $HOME-scoped
# lock, i.e. serialisation -- see realisateur#46 for the metabolism/quota split
# that separates tempo from the ceiling properly.
#
# ABSOLUTE PATH, deliberately. This file is parsed with `while IFS='|' read`,
# NOT sourced, so "$HOME" would arrive as five literal characters. Its sibling
# schedule/<project>.conf files ARE sourced and DO expand $HOME. One repo, two
# conventions, and neither file shows the difference -- MONKEY.md section 4.
#
# WEIGHT 1, not the 2 mandark's row carried. Weight is a share of the
# rotation; with a single participant it means nothing, and carrying a 2
# across would forge a judgment about a pre-migration ecosystem (the rule
# _paced.dexter.conf's own scheduler block states: "no weights remove").
#
# `bin/scheduler-run` is the generic entrypoint and the correct caller here:
# ecosim.conf and bibliothecaire.conf both set BATCH_PROMPT and no
# BATCH_SCRIPT, and scheduler-run REFUSES a tier that still has a legacy
# wrapper, so a wrong call fails loud.
#
# Each row's path is that PROJECT ACCOUNT's own clone. It is not a typo that
# the same script appears twice under different homes: the runner is invoked
# by the project user's crontab, as that user, against that user's checkout.
ecosim|1|1|/home/ecosim/Documents/Projects/scheduler/bin/scheduler-run ecosim batch
bibliothecaire|1|1|/home/bibliothecaire/Documents/Projects/scheduler/bin/scheduler-run bibliothecaire batch
# vim-arcade -- account #4, Zach-directed 2026-08-04: the next participant to
# dispatch from monkey, INSTEAD of chezz (chezz stays registered and staged,
# see its row below, deliberately left at 0). Landed end to end the same day
# via bin/setup-selfdev-project.sh vim-arcade --apply: uid 3000 (lowest free
# in the 3000-3099 band; the account itself already existed as PR #27's own
# witness run), home 0700, linger on, no sudoers entry, claude + gh
# credentials copied and witnessed live (`claude -p 'reply ok'` under a
# cron-shaped stripped environment), four per-repo deploy keys (read-only
# realisateur/scheduler/senechal, read-write vim-arcade, each proven with a
# live `git ls-remote` witness), landed at scheduler HEAD 874a58e with 0 BAD
# and a clean (zero "ERROR [") sync-crontab.sh preview. Full account record,
# including the one bug this run found and fixed, in
# realisateur/MONKEY.md 8.3.
vim-arcade|1|1|/home/vim-arcade/Documents/Projects/scheduler/bin/scheduler-run vim-arcade batch
# chezz -- LEFT PARKED ON PURPOSE (Zach-directed 2026-08-04: vim-arcade goes
# next, not chezz). Still registered and staged, one flag short of armed, and
# the 0 is the only thing standing between this and armed. Added 2026-08-04
# with schedule/chezz.conf; the unix account does not exist yet on monkey
# because creating it needs root and a human sitting has not been spent on it.
#
# It is 0 rather than 1 for a concrete reason, not caution: an ENABLED row
# whose command path does not exist makes every runner on this host log
#   SKIP chezz -- command not runnable: /home/chezz/.../scheduler-run chezz batch
# on EVERY tick. At the 30-minute tick this host now runs, that is ~48 lines a
# night of a failure that is not one, written into the same run.log being read
# to see how the corpus research went. Flip to 1 after the account exists.
#
# PARKED 2026-08-06 (enabled 1 -> 0), Zach-directed: "For now, pause chezz for
# 1 week." UN-PAUSE ON OR AFTER 2026-08-13 by flipping this row's second field
# back to 1 AND un-commenting the `EXEMPT: chezz@monkey` line in
# schedule/FREEZE -- both, or it stays dark.
#
# The reason, in his words, is not quota and not misbehaviour: "chezz needs
# special treatment. It deserves an audit of what's going wrong. Raising the
# turn ceiling does not address the problem of batch jobs being too large. This
# may mean splitting up chezz batches into different types of work." The week
# is time for that audit; un-pausing without doing it spends the week for
# nothing.
#
# One finding the pause pass turned up, because it bears on that audit: in the
# chezz account on monkey the Playwright suite CANNOT RUN AT ALL --
# `libatk-1.0.so.0` is missing, so 123 of 152 tests fail identically on a clean
# main, in 2-3ms, before any assertion. chezz's pre-commit hook runs that suite
# for any gameplay change, so no gameplay commit can land in that account, and
# each night's work survives only until the next `reset --hard` stashes it.
# Two such stashes were replayed to a branch on 2026-08-06.
#
# The block ABOVE this one is stale and is left standing as evidence rather
# than rewritten: it says chezz is "deliberately left at 0" and that the unix
# account "does not exist yet on monkey". Both stopped being true in ca6607d on
# 2026-08-05, and the row was 1 when this pause was applied.
#
# PARKED, NOT DELETED. Rotation suppression keys on rotation MEMBERSHIP, not on
# the enabled flag (schedule/_paced.conf header). Deleting this row would
# un-suppress the fixed nightly cron line that schedule/chezz.conf's
# BATCH_JOB_NAME/BATCH_CRON installs -- i.e. deleting it ARMS chezz rather than
# disarming it. That trap has fired before; crt's block below records it.
chezz|0|1|/home/chezz/Documents/Projects/scheduler/bin/scheduler-run chezz batch
# crt -- account #5, Zach-directed 2026-08-05 ("get dev off mandark tonight").
# The sprint doc's own readiness table (SPRINT-RECORD section 9) ranks crt
# first of the five registered-not-armed: the richest offline backlog, and its
# recorded blocker was DEXTER's spend limit, which stopped applying when dexter
# was cleared.
#
# Preconditions checked on the host rather than inferred from this repo: the
# account exists, its scheduler clone is at origin/main with a CLEAN TRACKED
# TREE (a dirty tracked file silently freezes a dispatcher -- SPRINT-RECORD
# section 4b), the brief is present at .claude/FOCUS.md, the default branch
# `main` agrees with origin/HEAD (the groc-mangr#2 trap), and
# `claude -p 'reply ok'` answered as user crt under a stripped, cron-shaped
# environment.
#
# WHY ONLY ONE ROW AT FIRST, when two accounts were authorised. usage-gate.sh
# read 7d util=0.540 against the sprint's own 0.60 hard floor -- six points of
# headroom for a dispatcher whose per-run cost has NEVER BEEN MEASURED, because
# crt had never dispatched. The gate reading is the reason, written down rather
# than left as unexplained caution -- which is what section 2 rule 2 asks for.
#
# SUPERSEDED THE SAME SESSION, and marked rather than deleted. This block used
# to end "baudin was authorised and is deliberately NOT here; it is next, after
# crt's first tick shows what one run of an unmeasured project actually costs."
# Zach merged #45 and directed baudin immediately ("merged both, arm baudin
# too"), so baudin's row is below and crt's first tick has NOT yet priced a
# run. That is a human overriding a pacing rule he owns, not the rule failing
# -- but the rule's condition genuinely was not met, and a reader deserves to
# see that rather than find prose that quietly agrees with whatever happened.
# The gate was re-probed before adding baudin and still read 7d util=0.540.
#
# This row also SUPPRESSES the fixed nightly cron line that schedule/crt.conf's
# newly-set BATCH_JOB_NAME/BATCH_CRON would otherwise install. The two changes
# are ONE change; splitting them re-creates the 2026-08-05 five-stray-cron-
# lines bug that crt.conf's own header records.
#
# PARKED 2026-08-06 (enabled 1 -> 0), Zach-directed: "CRT also needs to pause
# pending Zach work." UN-PAUSE CONDITION IS A PERSON, NOT A DATE -- there is no
# 2026-08-13 here and no other trigger to wait for; it stays 0 until Zach says
# the work he has in mind is done. Un-pausing means flipping this field back to
# 1 AND un-commenting `EXEMPT: crt@monkey` in schedule/FREEZE.
#
# Related, from the same reply and recorded rather than acted on: Zach's
# Decision 2 puts crt's end-products (potato, the whisper server) on a separate
# VM/Docker/WSL rather than on the self-dev host. That is a bigger change than
# this pause and is not made here.
#
# The "SUPERSEDED THE SAME SESSION" note above stands -- crt's first tick never
# did price a run, and now will not until the pause lifts. Its per-run cost
# remains unmeasured, so a reader restoring this row is still arming something
# whose cost nobody has measured.
#
# PARKED, NOT DELETED, and here more sharply than anywhere else in this file:
# this row is what SUPPRESSES the fixed nightly cron line that
# schedule/crt.conf's BATCH_JOB_NAME/BATCH_CRON installs (see the block above).
# Deleting the row to "clean up" would install that fixed nightly line and
# dispatch crt on a schedule this file no longer controls. That is the
# 2026-08-05 five-stray-cron-lines bug, and it is the reason for the rule.
crt|0|1|/home/crt/Documents/Projects/scheduler/bin/scheduler-run crt batch
# baudin -- account #6, Zach-directed 2026-08-05 in the same session as crt,
# immediately after merging #45: "merged both, arm baudin too". Second on the
# sprint doc's readiness table (SPRINT-RECORD section 9): issue #1 plus
# CONTRACT.md's two named "cheapest summons". Milestone hardware-blocked, but
# the software work is reachable by an agent, which is the bar for arming.
#
# Preconditions probed on the host, not inferred from this repo: account
# exists, scheduler clone up to date with a CLEAN TRACKED TREE, brief present
# at .claude/FOCUS.md, and `claude -p 'reply ok'` answered as user baudin under
# a stripped, cron-shaped environment.
#
# ITS DEFAULT BRANCH IS `master`, NOT `main` -- and origin/HEAD agrees, so the
# agent reads and pushes the same branch its brief lives on. Checked explicitly
# rather than assumed, because this is the groc-mangr#2 trap: there the default
# branch is a FEATURE branch nine commits behind the branch holding the current
# brief, so an armed agent would work from a five-day-stale FOCUS.md whose
# already-answered questions still read as open, and push to the feature
# branch. "Does the default branch match where the brief lives?" is free to
# check from outside and invisible from inside a conf.
#
# SIX DISPATCHERS NOW SHARE ONE QUOTA, and this file's own header is the
# warning to read before adding another: the lock is $HOME-scoped, so per
# ACCOUNT, and nothing on this host serialises them; USAGE_CEILING=0.99 and
# USAGE_RUSH_BEFORE_RESET_MIN=10080 mean the pacing hold is permanently off and
# a 1% ceiling is the only real stop. Adding rows scales how many things ask
# permission, not what they are asking about.
#
# CORRECTED WITHIN THE HOUR, same session, and left visible. This block first
# ended: "The next person adding row #7 should lower
# USAGE_RUSH_BEFORE_RESET_MIN toward its documented 120 first, or serialise the
# accounts host-wide." The first half of that is wrong and would do real harm
# -- see the DO NOT ACT ON THAT LAST CLAUSE block in this file's header. That
# value is Zach's documented 2026-07-29 decision, made because even-burn pacing
# had stalled both hosts; every restored-pacing value probed at 7d util=0.55
# returns verdict=HOLD, i.e. it would STOP all six dispatchers rather than pace
# them.
#
# The second half stands: serialisation (the $HOME-scoped lock) is the genuine
# open residual, and realisateur#46 is the filed fix that separates tempo from
# the ceiling instead of overloading one knob with both.
#
# The reason this correction is here rather than a silent edit: I wrote the
# wrong advice while quoting this file's own header at face value, and only
# found it by reading _usage.conf, which is the authoritative source for quota
# policy. A rotation file's alarm about a quota knob is not the decision record
# for that knob.
#
# PARKED 2026-08-06 (enabled 1 -> 0), Zach-directed, and this one is INDEFINITE:
# "Baudin should come offline until I can figure out what it is and what it's
# supposed to do. A run blocked on human-input should stop triggering."
#
# There is no date and no automatic trigger. The un-pause condition is that
# Zach decides what baudin is for. Anyone tempted to flip this back because a
# week has passed, or because the account looks idle and harmless, is
# un-pausing a project whose PURPOSE its owner says he cannot yet state -- and
# the second sentence above is the general principle he drew from it, which
# applies past baudin.
#
# It was armed for roughly 24 hours, 2026-08-05 to 2026-08-06, and its per-run
# cost was never measured -- the block above says as much and it is still true.
#
# Un-pausing means flipping this field back to 1 AND un-commenting
# `EXEMPT: baudin@monkey` in schedule/FREEZE.
#
# PARKED, NOT DELETED -- same reason as chezz and crt above: suppression keys
# on rotation membership, not on this flag, so removing the row can arm what it
# looks like it is removing.
baudin|0|1|/home/baudin/Documents/Projects/scheduler/bin/scheduler-run baudin batch
