# Participants for usage-paced-runner.sh -- HOST-SCOPED to `vaporwave`. Format
# and semantics: schedule/_paced.conf's header. WHY THIS FILE EXISTS: without
# it the runner falls back to that shared file, which is mandark's rotation --
# 0 enabled rows, so every tick on this host was inert and said nothing.
#
# THIS HOST DRAWS ON A DIFFERENT QUOTA. vaporwave has its own Anthropic
# account (realisateur#695); mandark, dexter and monkey share the estate's.
# A row here does not compete with a row in _paced.monkey.conf.
#
# The `1` is MEMBERSHIP, not arming: schedule/ROSTER's state column is the only
# surface that arms a project (#364), and all four rows there land `parked`.
dog|1|/home/dog/Documents/Projects/scheduler/bin/scheduler-run dog batch
vibe-kiosk|1|/home/vibe-kiosk/Documents/Projects/scheduler/bin/scheduler-run vibe-kiosk batch
inv-redirects|1|/home/inv-redirects/Documents/Projects/scheduler/bin/scheduler-run inv-redirects batch
vkv23|1|/home/vkv23/Documents/Projects/scheduler/bin/scheduler-run vkv23 batch
