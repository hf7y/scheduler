# Paced-runner tick meta -- HOST-SCOPED to `mandark`. INERT: it blanked
# RUNNER_CRON, retired 2026-08-22 (#81), and nothing sets that now. Kept only
# because tests/runner-conf-host-witness.sh case 6 sources it by name, and
# mandark is being taken offline 2026-09-08 -- when it goes, so does this.
