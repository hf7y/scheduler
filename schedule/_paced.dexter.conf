# Participants for usage-paced-runner.sh -- HOST-SCOPED to `dexter`.
#
# Same format as schedule/_paced.conf (name|enabled|command, or
# name|enabled|weight|command). See that file's header for the field
# semantics; they are not repeated here.
#
# WHY THIS FILE EXISTS (2026-07-24, dexter bring-up -- see DESIGN-NOTES.md
# "dexter registers itself as a second host"):
# dexter and mandark are two INDEPENDENT schedulers sharing one git-tracked
# repo (decided 2026-07-24: full local peer, no shared lock, no cross-host
# rotation). They therefore need different rotations. Rather than add a HOST
# column to the single shared _paced.conf -- one file two machines would edit,
# which the AUTOMATED weight-audit.sh writer makes an active conflict, not a
# theoretical one -- each host may own its own file:
#
#   schedule/_paced.<short-hostname>.conf   picked up automatically by that host
#   schedule/_paced.conf                    used by any host without its own file
#
# mandark has no _paced.mandark.conf, so it keeps reading _paced.conf exactly
# as it did before -- this file changed nothing there. Resolution order and
# the reasoning live in bin/usage-paced-runner.sh's "which participants file"
# block. List registered hosts with: ls schedule/_paced.*.conf
#
# dexter writes ONLY this file. mandark writes ONLY _paced.conf. That is the
# whole concurrency story -- different paths, so the two hosts cannot produce
# conflicting edits to the same lines.
#
# ---------------------------------------------------------------------------
# HOST POLICY (REVERSED 2026-07-28, Zach-directed, interactive; recorded by
# realisateur `/ideate`): **dexter is the DEFAULT execution host.** Move
# everything that can move. A project stays on mandark only for a STATED
# reason, and "it has no dexter-specific need" is no longer such a reason.
#
# STILL TRUE, and NOT reopened by the reversal: "many parallel jobs on dexter
# via VMs/WSL" remains explicitly declined (too far ahead, Zach 2026-07-24).
# The accepted quota-race risk between the two hosts is also unchanged --
# watch run.log on both boxes. Default-to-dexter is about WHERE work runs,
# not about how much runs at once.
# ---------------------------------------------------------------------------
#
# `scheduler` is deliberately NOT here. dexter could technically self-develop
# too, but two hosts independently committing to one scheduler git history --
# each auto-merging to its own local main -- is a stronger version of the
# divergence that bit this repo earlier the same day (two worktrees on ONE
# host). That one was cleared by a fast-forward, which only works while one
# side hasn't independently advanced -- exactly what two pushing hosts remove.
# Open human question in .scheduler/QUESTIONS.md; self-dev stays single-host
# until it is answered.
#
# Zach note. Scheduler may move to dexter eventually 260728
#
# ---------------------------------------------------------------------------
# crt -- PINNED here (hardware-evidenced), but enabled=0: NOT YET RUNNABLE.
#
# The pin is correct and re-verified: crt's OctoPrint at 192.168.0.43 answers
# from THIS WSL2 environment (checked 2026-07-24 from dexter: ICMP 0% loss,
# TCP 80 open, and an OctoPrint-identifying HTTP 302 to /login/ carrying the
# x-clacks-overhead header -- not merely a port that happened to be open).
# That retires the caveat that the 2026-07-20 confirmation was against the
# old full VM's networking and might not survive WSL2's NAT. It does.
#
# What is NOT satisfied is repo access. schedule/crt.conf sets
#   REPO_URL="/home/zach/git-remotes/crt.git"
# a bare repo on MANDARK's filesystem, deliberately local so crt's VM
# password never leaves that machine. dexter cannot reach that path, and
# crt had no remote mirror (crt.conf noted a deploy key existed "if a
# private GitHub mirror is wanted later" -- never set up). Verified by
# running it, not assumed:
#   $ bin/scheduler-run crt batch
#   fatal: repository '/home/zach/git-remotes/crt.git' does not exist
#
# UPDATED 2026-07-24, mandark side (human + interactive session on mandark):
# crt.conf's REPO_URL now points at
# ssh://mandark-lan/home/zach/git-remotes/crt.git -- SSH access provisioned
# on mandark (openssh-server installed+enabled, a dedicated
# `dexter_mandark_deploy` key restricted to git-shell only in mandark's
# authorized_keys, no shell/port-forwarding). This keeps the VM password on
# the LAN, same reasoning as the original local-path choice; "dexter becomes
# authoritative for crt instead of mandark" is the eventual direction but
# deliberately parked (see QUESTIONS.md) -- this is the smaller, reversible
# step. Full writeup: DESIGN-NOTES.md 2026-07-24.
#
# LIVE-VERIFIED 2026-07-24, from dexter itself:
#   $ git ls-remote ssh://mandark-lan/home/zach/git-remotes/crt.git
#   d1041d511ffe3eee00c7623d255bed39e32f2b20        HEAD
#   d1041d511ffe3eee00c7623d255bed39e32f2b20        refs/heads/main
# Host key fingerprint (SHA256:L8eHLUaeERW+6p428gZ+V6LjoHqUAlYvuTgEXQyD3Eg)
# cross-checked against mandark's actual /etc/ssh/ssh_host_ed25519_key.pub,
# confirmed a match -- not just "a host answered," actually mandark.
# crt is now enabled below.
#
# The command below is the GENERIC entrypoint, not mandark's
# ~/.local/bin/crt-nightly-batch-loop.sh wrapper: crt.conf sets BATCH_PROMPT
# and no BATCH_SCRIPT, so bin/scheduler-run is the correct caller and no
# per-host wrapper needs to exist here at all.
#
# schedule/_paced.conf's crt line was already dropped (merged 2026-07-24)
# ahead of this flip -- the accepted "crt runs nowhere" gap between that
# drop and live verification above is now closed.
crt|0|0|/home/zach/scheduler/bin/scheduler-run crt batch

# ---------------------------------------------------------------------------
# wtul -- NAMED EXCEPTION, added 2026-07-25 (see policy note above). Pure
# GitHub remote (REPO_URL="git@github-wtul-deploy:hf7y/wtul.git" in
# schedule/wtul.conf), no LAN/local-bare-repo dependency, no live physical
# device it acts on unattended (AUTONOMY_TIER="low", and its own batch
# prompt already refuses to touch the real optical drive unattended) -- the
# lowest-risk possible pick for a first non-hardware dexter test. Same weight
# (2) as its mandark line, same reasoning (in-progress momentum, 46
# commits/7d per the 2026-07-24 weight-audit pass). Uses the generic
# `bin/scheduler-run` entrypoint like crt above, NOT mandark's
# `~/.local/bin/wtul-batch-loop.sh` wrapper -- that wrapper is a host-local
# script installed by `sync-crontab.sh --apply` (mandark-only, not
# host-scoped, see the crt block's own note on this), and `scheduler-run`
# doesn't consult wtul.conf's BATCH_SCRIPT field at all, so bypassing the
# wrapper is safe, not a behavior change.
#
# `schedule/_paced.conf`'s wtul line was dropped in the SAME change as this
# one (see that file) so nothing double-dispatches -- same crt precedent.
#
# PARKED enabled=0 2026-07-25, same day it landed -- CREDENTIAL GAP, verified
# by running it on dexter, not assumed:
#   $ git ls-remote git@github-wtul-deploy:hf7y/wtul.git
#   ssh: Could not resolve hostname github-wtul-deploy: Temporary failure in
#        name resolution
#   fatal: Could not read from remote repository.
# The failure is name resolution, not auth: dexter's ~/.ssh/config has no
# `Host github-wtul-deploy` block at all (it has only github-scheduler-deploy
# and mandark-lan), and no wtul key exists here. Unlike crt -- which got a
# dedicated `dexter_mandark_deploy` key provisioned 2026-07-24 BEFORE its
# enabled=1 flip -- wtul's move here landed ahead of its credential, so the
# preconditions the crt block above established as the pattern ("live-verified
# from dexter itself" before flipping) were never met for wtul.
#
# Landing it enabled here while `_paced.conf` has it dropped means wtul runs
# NOWHERE and burns a gate probe + a failing clone every rotation slot -- the
# exact hazard `_paced.conf`'s own "DO NOT LAND THIS ALONE" note (crt block)
# warns about. So: parked here, restored to enabled=1 in `_paced.conf` in the
# SAME change, per the revert instructions both files already carry. wtul
# keeps running on mandark, unaffected.
#
# TO UN-PARK (the named-exception test is still wanted, only blocked).
# Steps 1 and 2 are DONE on dexter as of 2026-07-25; only step 3 (human,
# GitHub-side) gates the rest.
#
#   1. [DONE 2026-07-25] Dedicated keypair generated ON DEXTER, no passphrase,
#      scoped to wtul only, same shape/naming as the crt key:
#        ssh-keygen -t ed25519 -f ~/.ssh/dexter_wtul_deploy -N '' \
#                   -C 'dexter-wtul-deploy'
#      Fingerprint: SHA256:PF2dUFFdzCvD4lZ/Mhcd2Kkf9m103pt0kWxb9ov/QLA
#      The public half is deliberately NOT reproduced here line-wrapped --
#      a key transcribed off a wrapped comment is a key that silently
#      doesn't match. Copy it verbatim from the source of truth:
#        cat ~/.ssh/dexter_wtul_deploy.pub        (on dexter)
#      or the single-line copy in .scheduler/QUESTIONS.md's 2026-07-25 entry.
#      (Private half stays in ~/.ssh on dexter, mode 600, never tracked here.)
#
#   2. [DONE 2026-07-25] `Host github-wtul-deploy` block added to dexter's
#      ~/.ssh/config (HostName github.com, User git, IdentityFile
#      ~/.ssh/dexter_wtul_deploy, IdentitiesOnly yes). Verified wired, not
#      assumed -- the failure mode moved from DNS to auth, which is exactly
#      what "dexter's half is done, GitHub's half isn't" looks like:
#        before: ssh: Could not resolve hostname github-wtul-deploy
#        after:  git@github.com: Permission denied (publickey)
#
#   3. [BLOCKED -- HUMAN, needs repo admin] Add the step-1 public key as a
#      Deploy Key on the hf7y/wtul GitHub repo with WRITE access (wtul's
#      batch job pushes), restricted to that one repo -- same minimal-scope
#      precedent as crt's key. Must be done in the GitHub UI: `gh` is not
#      installed on dexter at all (`gh: command not found`, checked
#      2026-07-25), so there is no authenticated path to
#      `gh repo deploy-key add` from here.
#
#   4. LIVE-VERIFY before flipping, same bar crt was held to -- expect refs,
#      not a permission error:
#        git ls-remote git@github-wtul-deploy:hf7y/wtul.git
#
#   5. Only then flip this line to 1 AND drop `_paced.conf`'s wtul line back
#      to 0 in the same change.
#
# Do NOT flip this line on the strength of steps 1-2 alone: the key existing
# on dexter is not the same fact as GitHub accepting it, and step 4 is the
# only thing that distinguishes them.
# Tracked as a dated entry in .scheduler/QUESTIONS.md.
wtul|0|0|/home/zach/scheduler/bin/scheduler-run wtul batch

# ---------------------------------------------------------------------------
# scheduler -- B4 of THE PLAY, added 2026-07-29 (Zach-directed via realisateur
# /ideate: "b4, add scheduler to the dexter rotation"). Deliberately deferred
# until the bootstrap PROVED OUT rather than added on faith; Zach 2026-07-29:
# "b4 install it once the bootstrap play is installed."
#
# THE BOOTSTRAP THAT EARNED THIS LINE, verified from mandark off the ref
# (`git ls-remote` / `merge-base --is-ancestor`), not from dexter's own report:
#   d62e961  bin/scheduler: locate its own checkout, and refuse loudly ...
#   46f51ae  tests: reconcile-witness reads the tree it ships in ...
#   5bde4d5  Merge branch 'paced/2026-07-29'      <- all authored "Dexter Pine"
# A real cycle, run BY HAND on dexter, that committed and pushed FROM dexter.
# That was the stated proof bar and it is the only reason this line exists.
#
# NOTE ON THE COMMAND PATH: the repo path, not ~/.local/bin/, because dexter
# has no installed wrapper -- unlike mandark. Also: this script needs `claude`
# on PATH, which cron supplies via the crontab's own PATH= line and a
# non-interactive ssh does NOT (~/.bashrc returns early). The first hand-run
# bootstrap died on exactly that: "line 295: claude: command not found".
#
# SINGLE-WRITER IS NOW PERMANENT -- RESOLVED 2026-07-29, same day this warning
# was written. It previously read that mandark still had `scheduler|1|3`
# enabled and that releasing schedule/FREEZE would put two hosts on one
# scheduler history within a tick. THAT IS NO LONGER TRUE and the old text is
# replaced rather than left standing, because a stale warning is worse than
# none: it trains readers to discount the file.
#
# Zach answered BLOCKERS.md 63cf3b4 decision (1) this date -- "confirm
# 63cf3b4, mandark self-dev goes dark" -- and mandark's scheduler line in
# schedule/_paced.conf is now commented out. Dexter is the ONLY host that
# auto-commits scheduler's own history, as a property of the rotation files
# themselves rather than of a freeze file that is meant to be temporary.
#
# CONSEQUENCE: schedule/FREEZE may now be released without re-creating the
# two-writer hazard. Its `EXEMPT: scheduler@dexter` line is still correct but
# is no longer what is holding single-writer.
#
# STILL TRUE: scheduler-dev-cycle.sh "merges into local main AND PUSHES TO
# ORIGIN IMMEDIATELY, same cycle". Re-enabling mandark self-dev means
# answering the two-writer question again, from scratch.
scheduler|1|3|/home/zach/scheduler/bin/scheduler-dev-cycle.sh

# ---------------------------------------------------------------------------
# realisateur -- the SECOND and LAST participant of THE PLAY's re-run
# (schedule/RUN-MARKER: "registers exactly two participants (scheduler,
# realisateur)"). Added 2026-07-29 by scheduler's own paced cycle ON DEXTER,
# against .scheduler/FOCUS.md's bootstrap brief: "Register realisateur into
# the rotation and witness one real dispatch."
#
# The 2026-07-29 10:00 cycle deliberately SKIPPED this and said why: adding a
# participant dexter cannot reach is what this file's own crt/wtul blocks
# forbid, and realisateur.conf then pointed at a remote dexter had no path to.
# `d35f49f` repointed it to https://github.com/hf7y/realisateur.git, which is
# what makes this landable now. The precondition did not lapse; it was met.
#
# LIVE-VERIFIED FROM DEXTER, under a cron-shaped environment (no ssh-agent, no
# terminal prompt), because "the repo exists" and "this host can drive it" are
# different facts and only the second one matters here:
#
#   # verified 2026-07-29 via: env -u SSH_AUTH_SOCK GIT_TERMINAL_PROMPT=0 \
#   #   GIT_SSH_COMMAND="ssh -o BatchMode=yes" \
#   #   git ls-remote https://github.com/hf7y/realisateur.git HEAD
#   30dd0db...  HEAD                                   <- READ path works
#
#   # verified 2026-07-29 via: git push --dry-run origin \
#   #   HEAD:refs/heads/zz-auth-probe-not-created      (in a /tmp clone)
#   * [new branch]  HEAD -> zz-auth-probe-not-created  <- WRITE path works
#
# The second probe is the one wtul's block above exists to insist on: wtul was
# landed on the strength of a key EXISTING and sat parked for four days because
# GitHub had never been asked to accept it. A `--dry-run` push authenticates
# against git-receive-pack and creates nothing -- confirmed after the fact with
# `git ls-remote ... 'refs/heads/zz-auth-probe*'`, which came back empty.
# Auth is dexter's gh credential helper (`!/usr/bin/gh auth git-credential`,
# token in ~/.config/gh/hosts.yml with `repo` scope) -- a FILE, not a keyring,
# which is why it survives cron with no session bus.
#
# NO WEIGHT FIELD, AND THAT IS DELIBERATE -- it is not an omission to be
# tidied up later. mandark's line carried |3|. Carrying that 3 across would
# forge a judgment: Zach 2026-07-29 ("no weights remove") had realisateur
# reweight the ecosystem fresh AFTER the migration, and schedule/_paced.conf's
# own scheduler block states the rule for exactly this case -- "Do not restore
# a 3 here from memory -- that is a frozen judgment about a pre-migration
# ecosystem." Omitted = the documented default of 1. scheduler enforces
# weights, it never sets them; realisateur assigns its own when it reweights.
#
# COMMAND PATH: the repo entrypoint, not ~/.local/bin/ -- dexter has NO
# installed wrappers (`ls ~/.local/bin` has no *-loop.sh at all, checked this
# date), same reason the crt/wtul/scheduler lines above use it. Verified
# runnable rather than assumed: realisateur.conf sets BATCH_JOB_NAME and
# BATCH_PROMPT and no BATCH_SCRIPT, which is precisely the shape
# bin/scheduler-run requires (it REFUSES a tier that still has a legacy
# wrapper, so a wrong call here would fail loud, not double-run).
#
# INERT UNTIL THE FREEZE LIFTS, by design -- bin/scheduler-run consults
# freeze-check.sh at dispatch, and realisateur is NOT in the EXEMPT line:
#   $ bin/freeze-check.sh realisateur   ->  rc=1, "FROZEN -- dispatch refused"
# So this line changes nothing today. It is the registration; the brief's
# other half -- WITNESSING a dispatch -- cannot happen until the freeze is
# released, and must not be faked by exempting realisateur to force one.
#
# PAIRED, NOT LANDED ALONE (the rule the crt block states in caps): the
# matching `realisateur|1|3` line in schedule/_paced.conf is commented out in
# this same change. Two hosts dispatching one project is the confirmed-live
# double-run hazard, and realisateur pushes to a single GitHub history.
#
# Revert: delete this line and un-comment realisateur in _paced.conf. Both
# halves, or the double-dispatch comes back.
realisateur|1|/home/zach/scheduler/bin/scheduler-run realisateur batch

# NOTE: this rotation is NOT empty -- `scheduler` and `realisateur` are
# enabled above, so dexter's tick probes usage-gate.sh and dispatches. Watch
# run.log on both boxes per the still-open quota-race question above.
# CORRECTED 2026-07-29: this note previously read "with crt and wtul both
# enabled" -- both are `|0|` and have been since they were parked, so the
# sentence named the two participants that were NOT carrying the rotation.
# Re-probed, not quoted: `grep -E '^[a-z].*\|' schedule/_paced.dexter.conf`.
