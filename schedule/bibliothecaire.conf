# bibliothecaire -- the library: shelve, catalogue, retrieve, and now triage.
#
# REGISTERED 2026-08-02 to record a CLASSIFICATION, not to arm dispatch.
#
# Why this file exists at all. In this ecosystem the registry means "utility"
# (`realisateur/.scheduler/FOCUS.md`, W1). A verb is a utility's finished form;
# a product's finished form is an event outside the computer
# (`realisateur/WAITING-ROOM.md:10-18`). `bashify emit` and `bashify coin`
# already refuse a project with no conf here -- but only at MINT time, and
# never again, which is how `abletim` and `wtul` kept verbs after being
# deregistered. bibliothecaire declares 7 verbs and had no conf, so it read as
# a product carrying a verb surface. Zach classified it a utility on
# 2026-08-02; this file is that decision, mechanized.
#
# ---------------------------------------------------------------------------
# ARMED: NO. DELIBERATELY. Read this before adding a tier.
# ---------------------------------------------------------------------------
# Both tiers are blank and there is NO row for bibliothecaire in
# schedule/_paced.conf. Registration and dispatch are separate acts here:
# membership in _paced.conf is what arms a project, not the presence of this
# file. `bin/sync-crontab.sh` treats a blank tier as a legitimate state
# ("or leave the whole tier blank", bin/sync-crontab.sh:635), so this conf
# emits no cron line. Verified by diffing `sync-crontab.sh` preview output
# before and after this file was added: identical.
#
# It must stay that way for now. THE FLOOR's resumption contract is one
# project enabled at a time, and a second is unparked only after the first has
# completed seven consecutive clean nights. Nothing is armed today. Arming
# bibliothecaire is a separate, deliberate decision that belongs to that
# contract, not to this classification.
#
# ---------------------------------------------------------------------------
# THE OPEN ITEM, recorded here because it is the reason this is not simple
# ---------------------------------------------------------------------------
# bibliothecaire's `main` still carries a live, hardware-bound PRODUCT: a
# physical scanner writing to an SMB share, three systemd timers firing on
# schedule (intake every 15 min, OCR hourly, health daily), with OCR ceilings
# tuned to this machine's RAM after a real OOM kill. By the classifier's own
# words that is "an event outside the computer".
#
# The decision (Zach, 2026-08-02) is that the SCANNER is the product and should
# be parted off into its own repo, leaving bibliothecaire a core utility that
# is stable and non-agent. That extraction is DEFERRED, not done. Until it
# happens this conf describes a repo that is a utility by intent and a product
# by content, and that gap is the thing to close -- not to forget.

PROJECT="bibliothecaire"
PROJECT_KEY="bibliothecaire"

PROJECT_REPO_PATH="/home/zach/Documents/Projects/bibliothecaire"

REPO_URL="https://github.com/hf7y/bibliothecaire.git"
REPO_SUBDIR="."

# ===========================================================================
# TIER 1 -- bug sweeper: BLANK. No live web tracker, and nothing is armed.
# ===========================================================================
SWEEP_JOB_NAME=""

# ===========================================================================
# TIER 2 -- overnight batch: BLANK. See the ARMED block above; this is the
# knob that would arm it, and it is intentionally empty. Setting it is not
# enough on its own either -- a _paced.conf row is also required -- but it is
# the first half, so it is the first place to look if something ever dispatches
# bibliothecaire unexpectedly.
# ===========================================================================
BATCH_JOB_NAME=""

# The verbs this project declares, for the record. Derived, not authoritative:
# the declaration lives in the `bashified` branch (executable bin/<name> plus
# man/<name>.1) and is read by realisateur's bin/lib/verb-set.sh. Listed here
# so a human reading the registry can see what registering this project means.
#
#   accroche cueille fonde glane range trie verse
#
# `trie` arrived 2026-08-02 from `secretaire`, which was reclassified a product
# and had its bashified branch retired to the tag `parked/bashified`. It was
# called `range` there and collided with this library's own `range`; the move
# dissolved the collision rather than picking a winner.
