GENERATION gen-2: no clones. Work that milestone only; its description is the scope bar. SETTLED,
cite and do not re-open: #303 ruled the entry point TAKES THE REPO AS AN ARGUMENT; #306 holds the
invariant and the acceptance test; #432 puts state AND config in a container -- never fix a dispatch
failure at the FETCH. realisateur#840/#996 CLOSED unmerged, superseded by MERGED #1094: the arming
service runs on dexter, 23 rows / 14 live. #684 MERGED -- fetch_roster reads it, roster_rows and
roster-arm-audit narrow by `getent passwd <project>`, `dose` REFUSES (7) off-host rather than hopping.
PROVED: reads need NO credential, from dog@vaporwave, wtul@monkey and root@monkey under `env -i` --
what an armed cron row is, and the wall host mode died on; `scheduler-run` and host mode both resolve
a full rotation from the installed build, no clone. NOT YET: nothing armed, no real dispatch, no
build carrying #684 or the `usage-gate.sh` NOT-A-VERB row. PROHIBITED: cutting or approving a build
is a human gate (#676). OPEN: #686 owns the WRITE; `--arm/--park` refuse, their roster synthesised.
