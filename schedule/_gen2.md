GENERATION gen-2: no clones. Work that milestone only; its description is the scope bar. SETTLED, cite
and do not re-open: #303 ruled the entry point TAKES THE REPO AS AN ARGUMENT; #306 holds the invariant
and the acceptance test; #432 puts state AND config in a container -- never fix a dispatch failure at
the FETCH. realisateur#840/#996 CLOSED unmerged, superseded by MERGED #1094: the arming service runs
on dexter. #684 MERGED -- fetch_roster reads it, roster_rows and roster-arm-audit narrow by `getent
passwd <project>`, `dose` REFUSES (7) off-host rather than hopping. #686 MERGED (#688) --
`--arm/--park` POST the service and re-read to confirm; no branch, no PR, no schedule/ROSTER. #690/#691
MERGED stale-header fixes. PROVED with no `gh` config at all: `dose-project.sh --check` runs clean, and
#350's four checkout sites are gh-dead-code on what `carry.sh` cuts. NOT YET: nothing armed for real, no
build carrying #684/#688/#690 or the `usage-gate.sh` NOT-A-VERB row -- a human gate (#676). OPEN: #350,
#570 wait on that gate and a DEFAULT-AFTER:none credential DECISION -- neither agent-actionable now.
