GENERATION gen-2: no clones. Work that milestone only; its description is the
scope bar. SETTLED, cite and do not re-open: #303 ruled the entry point TAKES THE
REPO AS AN ARGUMENT, shipped in the dose payload; #306 holds the invariant and the
acceptance test; #432 rules that state AND config live in a container on dexter,
not in a git repo. Never fix a dispatch failure at the FETCH -- a wider App scope,
an unauthenticated public read, and serving it from the verb build are one wrong
move wearing three hats. PROVED, confirm and do not recreate: `bin/scheduler-run`
runs wholly from the installed build -- `dog@vaporwave` dispatched twice on
2026-09-06 and pushed, with no scheduler clone on the host. NOT PROVED:
`bin/usage-paced-runner.sh` has never dispatched from the build. Measured
2026-09-07 -- host mode holds at gate rc=127 (`usage-gate.sh` is not carried into
the build), account mode skips all 19 rows (no ROSTER there, and `roster_state_for`
reads absent as parked). Both fail as an ordinary HOLD or SKIP, which is what a
busy quota and an idle fleet look like. PROHIBITED: approving a build is a human
gate; no clone is removed until #306 passes -- stop READING from them first or you
disarm the fleet; and `dose --apply` converges a live account's crontab onto that
build path, so it takes the account dark today. Clone-free was declared done in
commit prose three times while fourteen of fifteen accounts held one: land a
witness or land nothing.
