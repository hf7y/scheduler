GENERATION gen-2: no clones. Work that milestone only; its description is the scope bar. SETTLED,
cite and do not re-open: #303 ruled the entry point TAKES THE REPO AS AN ARGUMENT; #306 holds the
invariant and the acceptance test; #432 puts state AND config in a container -- never fix a dispatch
failure at the FETCH. realisateur#840/#996 CLOSED unmerged, superseded by MERGED #1094: the arming
service runs on dexter, seeded 23 rows / 14 live. #684 MERGED -- fetch_roster reads it, roster_rows
and roster-arm-audit narrow by `getent passwd <project>`, and `dose` REFUSES (7) off-host instead of
hopping, because there is no host column left to hop by. PROVED, confirm and do not recreate: reads
need NO credential, from dog@vaporwave, wtul@monkey and root@monkey under `env -i` -- which is what
an armed cron row is, and the wall host mode died on. `scheduler-run` and host mode both resolve a
full rotation from the installed build with no clone. NOT YET: no real dispatch and nothing armed --
no build carries #684, and `bin/usage-gate.sh` reaches a build only now that it has a NOT-A-VERB
manifest row. PROHIBITED: cutting or approving a build is a human gate; #676 makes `dose --apply`
refuse onto a build that cannot dispatch. OPEN: #686 owns the WRITE -- `--arm/--park` refuse until
it lands, because the roster they hold is synthesised and committing it would be a fabrication.
